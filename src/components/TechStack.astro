---
interface Props {
  items: string[]
}
const { items } = Astro.props
---

<ul class="flex flex-wrap items-center justify-center gap-2">
  {items.map((item) => (
    <li class="rounded-sm bg-surface px-2 py-1 font-mono text-sm font-medium">{item}</li>
  ))}
</ul>
